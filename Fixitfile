pipeline {
    agent any
    stages {
        stage('Performing Actions') {
            steps {
                echo "Running ${params.OPERATION} on ${params.SERVICE_NAME}"
            }
        }  
    }
}